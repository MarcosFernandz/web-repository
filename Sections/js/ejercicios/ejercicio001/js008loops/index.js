const numeros = [10, 40, 44, 35, 45]

// function sumadeArrayDeNums(arrayDeNumero) {}

const sumaDeArrayDeNumsFlecha = (arrayDeNumero) =>{
    //Paso  variablepara guardar  suma
    let sumaTotal = 0

    // let sumaTotal

    //PASO iteracion
    for(let  i =  0;  i < arrayDeNumero. length; i++){
        //paso dentrodelbucle- guardar cada  iteracion delarray dentro    de  lavariable   sumaTotal :)
        sumaTotal += arrayDeNumero[i];

    }
    return  sumaTotal;

    //  PASO  retornar
};
sumaDeArrayDeNumsFlecha(numeros)

